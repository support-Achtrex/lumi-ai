const { GoogleGenerativeAI } = require('@google/generative-ai'); require('dotenv').config(); const ai = new GoogleGenerativeAI(process.env.GEMINI_API_KEY); const model = ai.getGenerativeModel({ model: process.env.GEMINI_MODEL || 'gemini-1.5-flash', systemInstruction: 'hi' }); model.generateContentStream({ contents: [{role:'user',parts:[{text:'hello'}]}]}).then(async (res) => { for await (const chunk of res.stream) { console.log(chunk.text()); } }).catch(console.error);
